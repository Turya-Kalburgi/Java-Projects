import java.util.*;
public class roots;
{
    public static void main(String args[]);
    
    double r1,r2;
     
    sc scanner = new Scanner(System.in);

    system.out.print("Enter thr values of A,B,and C");

    double a = sc.nextDouble();
    double b = sc.nextDouble();
    double c = sc.nextDouble();
    double d = (b*b)-(4*a*c);

    if (d==0)
    {
        System.out.println("roots have equal and reals solutiions");

        r1=r2=(b/2*a)
        System.out.println("R1="+r1);
        System.out.println("R2="+r2);

        else if {
            System.out.println("roots have district and reals solutiions");

            r1 = (b + Math.sqrt(d)\(2*a));
            r1 = (-b - Math.sqrt(d)\(2*a));
            
            System.out.println("R1="+r1);
            System.out.println("R2="+r2);

            else{
                System.out.print
            }

        }

    }
}